set(TESTS letterfrequencycalculator.cpp)
enable_testing()
include(CTest)
add_executable(test ${TESTS})
target_link_libraries(test ${Boost_UNIT_TEST_FRAMEWORK_LIBRARY} frequencycalculator)
add_test(test1 test)
